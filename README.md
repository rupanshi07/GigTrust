# GigTrust

**GigTrust** is a cloud-native freelance marketplace prototype designed to demonstrate how a modern application can combine **transaction-safe relational storage**, **polyglot persistence**, **containerized deployment**, **multi-region availability**, **health monitoring**, and **failover routing** on Microsoft Azure.

The project was built as a combined **Database Management Systems (DBMS)** and **Cloud Computing** academic project. The DBMS side focuses on ACID transactions, normalization, concurrency control, escrow workflows, activity logging, telemetry storage, and auditable transaction history. The Cloud Computing side focuses on Docker, Azure Container Registry, Azure Container Apps, Azure App Service, Azure Traffic Manager, health probes, multi-region deployment, and failover.

---

## 1. Project Overview

Freelance marketplaces must handle multiple categories of data and infrastructure requirements at the same time:

- user profiles and gig information
- bids and contracts
- escrow transactions
- concurrent access to shared transactional data
- audit and activity logs
- telemetry generated by deployed services
- regional application availability
- automatic failover during regional service disruption

GigTrust addresses these requirements using a **polyglot database architecture** and a **multi-region Azure deployment**.

### Main idea

The application backend is implemented using **FastAPI** and packaged as a Docker container. The same backend image is deployed in two Azure regions:

- **Central India** — Primary region
- **Korea Central** — Secondary/failover region

Azure Traffic Manager uses **Priority routing** so that requests resolve to the Central India backend during normal operation and switch to Korea Central when the primary endpoint becomes unavailable or is disabled.

The application uses:

- **Azure SQL Database** for structured transactional data
- **Cosmos DB for MongoDB** for activity logs
- **Cosmos DB for Cassandra** for telemetry and scaling-action history

---

## 2. Project Objectives

The main objectives of GigTrust are:

1. Build a freelance marketplace backend using FastAPI.
2. Maintain transaction consistency for escrow operations.
3. Demonstrate ACID properties and rollback.
4. Handle concurrent transactions safely.
5. Store structured and unstructured operational data using suitable database technologies.
6. Containerize the backend using Docker.
7. Store application images in Azure Container Registry.
8. Deploy the backend in multiple Azure regions.
9. Configure health-based priority routing using Azure Traffic Manager.
10. Demonstrate regional failover and recovery.
11. Store telemetry in Cassandra.
12. Record resource-management decisions such as `NO_ACTION`, `SCALE_UP`, and `REROUTE`.
13. Maintain an auditable history of important system actions.
14. Provide a foundation for future fraud detection and explainable AI modules.

---

## 3. High-Level Architecture

```text
                         ┌──────────────────────────────┐
                         │           Users              │
                         │ Client / Freelancer / Admin  │
                         └──────────────┬───────────────┘
                                        │
                                        ▼
                         ┌──────────────────────────────┐
                         │   Azure Traffic Manager      │
                         │     Priority Routing         │
                         │                              │
                         │ Priority 1 : Central India   │
                         │ Priority 2 : Korea Central   │
                         └──────────────┬───────────────┘
                              ┌─────────┴─────────┐
                              │                   │
                              ▼                   ▼
                ┌──────────────────────┐   ┌──────────────────────┐
                │ Central India        │   │ Korea Central        │
                │ Azure Container Apps │   │ Azure App Service    │
                │ FastAPI Backend      │   │ FastAPI Backend      │
                └──────────┬───────────┘   └──────────┬───────────┘
                           │                          │
                           └────────────┬─────────────┘
                                        │
          ┌─────────────────────────────┼─────────────────────────────┐
          │                             │                             │
          ▼                             ▼                             ▼
┌────────────────────┐       ┌────────────────────┐       ┌────────────────────┐
│ Azure SQL Database │       │ Cosmos DB MongoDB  │       │ Cosmos DB Cassandra│
│ Transactions       │       │ Activity Logs      │       │ Telemetry          │
│ Users / Gigs       │       │ Operational Events │       │ Scaling Actions    │
│ Bids / Contracts   │       │                    │       │                    │
│ Escrow             │       │                    │       │                    │
└────────────────────┘       └────────────────────┘       └────────────────────┘
```

---

## 4. Technology Stack

### Backend
- Python
- FastAPI
- Uvicorn

### Containerization
- Docker
- Docker Desktop
- Windows Subsystem for Linux (WSL)

### Microsoft Azure
- Azure Container Registry
- Azure Container Apps
- Azure App Service for Containers
- Azure Traffic Manager
- Azure SQL Database
- Azure Cosmos DB for MongoDB
- Azure Cosmos DB for Cassandra

### Database Connectivity
- `pyodbc` for Azure SQL
- `pymongo` for MongoDB
- Cassandra Python driver for Cosmos DB Cassandra API

### Development Environment
- Windows 11
- Visual Studio Code
- PowerShell
- Git / GitHub

---

## 5. Database Architecture

GigTrust uses a **polyglot persistence** model. Different types of information are stored in different database systems based on their access characteristics.

### 5.1 Azure SQL Database

Azure SQL is used for strongly structured and transactional data.

Typical relational entities include:

- Users
- Gigs
- Bids
- Contracts
- Escrow transactions
- Payments
- Transaction history

Azure SQL is suitable because marketplace transactions require:

- ACID guarantees
- referential integrity
- constraints
- transaction rollback
- concurrency control
- joins
- reliable financial-state updates

#### Example escrow workflow

```text
Client funds escrow
        ↓
Amount is marked as held
        ↓
Freelancer completes work
        ↓
Client verifies completion
        ↓
Escrow is released
        ↓
Transaction history is updated
```

If any step fails inside a transaction, the database operation can be rolled back so that partial financial updates are not committed.

### 5.2 Cosmos DB for MongoDB

MongoDB is used for flexible operational and activity logging.

Examples:

- user login events
- gig creation events
- bid submission events
- contract actions
- API activity
- fraud-related events
- audit metadata

A document database is suitable because activity records can evolve without requiring rigid relational schema changes.

Example document:

```json
{
  "user_id": 101,
  "event": "BID_SUBMITTED",
  "timestamp": "2026-08-18T10:30:00Z",
  "metadata": {
    "gig_id": 42,
    "bid_amount": 7500
  }
}
```

### 5.3 Cosmos DB for Cassandra

Cassandra is used for time-series-style operational telemetry.

Telemetry data can include:

- CPU utilization
- API latency
- request throughput
- node identifier
- deployment region
- timestamp

The Cassandra design uses a partition based on:

```text
(region, metric_type)
```

and clusters readings by timestamp.

This makes it efficient to retrieve recent metrics for a particular region and metric.

#### Telemetry table

```sql
CREATE TABLE telemetry_readings (
    region        TEXT,
    metric_type   TEXT,
    ts            TIMESTAMP,
    node_id       TEXT,
    value         DOUBLE,
    PRIMARY KEY ((region, metric_type), ts)
) WITH CLUSTERING ORDER BY (ts DESC);
```

#### Scaling actions table

```sql
CREATE TABLE scaling_actions (
    region          TEXT,
    ts              TIMESTAMP,
    action          TEXT,
    trigger_metric  TEXT,
    trigger_value   DOUBLE,
    threshold       DOUBLE,
    PRIMARY KEY (region, ts)
) WITH CLUSTERING ORDER BY (ts DESC);
```

This table provides an audit trail for decisions made by the resource-management agent.

---

## 6. DBMS Concepts Demonstrated

The project demonstrates several core DBMS concepts.

### ACID Transactions
- **Atomicity** — either all transaction steps succeed or none are committed.
- **Consistency** — database constraints and business rules remain valid.
- **Isolation** — concurrent operations do not corrupt each other's state.
- **Durability** — committed changes remain stored.

### Rollback
If an escrow transaction fails before completion, changes can be rolled back.

### Concurrency Control
The project considers scenarios where multiple operations access the same records concurrently, such as:

```text
Transaction A → releases payment
Transaction B → opens a dispute
```

Proper locking and transaction ordering are required to prevent inconsistent state.

### Deadlock Scenario
A deadlock can occur when two transactions acquire resources in different orders and wait for each other.

The project studies:
- deadlock creation
- deadlock detection
- transaction rollback
- consistent lock ordering as a prevention strategy

### Normalization
The relational model is designed with normalization in mind to reduce redundancy and update anomalies.

The project discusses:
- 1NF
- 2NF
- 3NF
- BCNF
- 4NF where multivalued relationships such as user skills or portfolio items are modeled separately

---

## 7. Backend API

The GigTrust backend is implemented using FastAPI.

FastAPI provides:
- REST API development
- automatic Swagger documentation
- JSON request/response support
- simple health endpoints
- asynchronous application support

During local development the application is typically available at:

```text
http://localhost:8000
```

Swagger documentation:

```text
http://localhost:8000/docs
```

When running through Docker with a host mapping such as:

```bash
docker run --rm -p 8001:8000 --env-file .env gigtrust-api
```

the application can be accessed at:

```text
http://localhost:8001/docs
```

---

## 8. Health Endpoints

The backend contains endpoints for verifying external service connectivity.

Examples include:

```text
/health/database
/health/mongodb
/health/cassandra
```

Expected Azure SQL response:

```json
{
  "database": "Azure SQL",
  "status": "connected",
  "test": 1
}
```

Equivalent health checks are available for MongoDB and Cassandra.

A future improvement is to create a unified `/health` endpoint that returns HTTP `200` only when required services are healthy and HTTP `503` when the application should be considered unavailable.

---

## 9. Docker Containerization

The FastAPI backend was containerized to make the application portable across local development and Azure.

### Build image

```bash
docker build -t gigtrust-api .
```

### Run locally

```bash
docker run --rm -p 8001:8000 --env-file .env gigtrust-api
```

Uvicorn runs inside the container on:

```text
0.0.0.0:8000
```

The browser accesses the exposed host port instead of `0.0.0.0`.

Example:

```text
http://localhost:8001/docs
```

---

## 10. Docker Build Issue Encountered

During the project, the Docker image build initially failed because of a package repository signing issue involving:

- Debian image version
- Microsoft Debian repository
- SHA1 signing policy

The Dockerfile/base image configuration was corrected so that the required ODBC driver and Python dependencies could be installed successfully.

This troubleshooting step was important because Azure SQL required `ODBC Driver 18 for SQL Server` inside the application container.

---

## 11. Azure Container Registry

The Docker image is stored in Azure Container Registry.

Registry:

```text
gigtrustacr24222
```

The secure registry login server used by the deployment is:

```text
gigtrustacr24222-b2bhamdhfhfth2gh.azurecr.io
```

Image repository:

```text
gigtrust-api
```

Tag:

```text
latest
```

Typical workflow:

```text
Dockerfile
   ↓
docker build
   ↓
Local Docker image
   ↓
docker tag
   ↓
docker push
   ↓
Azure Container Registry
   ↓
Azure compute service pulls image
```

---

## 12. Primary Deployment — Central India

The primary application deployment runs in **Central India**.

Service:

```text
Azure Container Apps
```

Container App:

```text
gigtrust-central
```

Environment:

```text
gigtrust-env-central
```

Application URL:

```text
https://gigtrust-central.yellowground-b72cfd5f.centralindia.azurecontainerapps.io
```

Target application port:

```text
8000
```

The Container App pulls the Docker image from Azure Container Registry and runs the FastAPI application.

---

## 13. Secondary Deployment — Korea Central

The original plan was to deploy a second Azure Container App in another region.

However, the Azure for Students subscription imposed a limit of one Container Apps Environment.

The second deployment therefore uses:

```text
Azure App Service for Containers
```

App Service:

```text
gigtrust-korea
```

Region:

```text
Korea Central
```

App Service Plan:

```text
gigtrust-plan-korea
```

Pricing tier used during implementation:

```text
Linux Basic B1
```

Application domain:

```text
https://gigtrust-korea-gzc3dbaqe8grbnf4.koreacentral-01.azurewebsites.net
```

The application container still uses the same image `gigtrust-api:latest`.

This demonstrates that the same containerized backend can run on different Azure compute products.

---

## 14. Environment Variables and Secrets

Configuration is supplied through environment variables rather than hard-coded inside the source code.

Example non-sensitive variables:

```env
SQL_SERVER=gigtrust-sql-24222.database.windows.net
SQL_DATABASE=gigtrustdb

MONGO_DATABASE=gigtrust
MONGO_COLLECTION=activity_logs

CASSANDRA_HOST=gigtrust-cassandra-24222.cassandra.cosmos.azure.com
CASSANDRA_PORT=10350
CASSANDRA_USERNAME=gigtrust-cassandra-24222
CASSANDRA_KEYSPACE=gigtrust
```

Sensitive values such as passwords and connection strings should never be committed to GitHub.

Examples:

```env
SQL_USERNAME=...
SQL_PASSWORD=...
MONGO_URI=...
CASSANDRA_PASSWORD=...
```

For Azure Container Apps, sensitive values were stored as application secrets and referenced through environment variables. For App Service, application settings were used.

---

## 15. Azure SQL Connectivity

SQL server:

```text
gigtrust-sql-24222.database.windows.net
```

Database:

```text
gigtrustdb
```

The backend connects using ODBC Driver 18.

Example connection logic:

```python
connection_string = (
    "DRIVER={ODBC Driver 18 for SQL Server};"
    f"SERVER=tcp:{server},1433;"
    f"DATABASE={database};"
    f"UID={username};"
    f"PWD={password};"
    "Encrypt=yes;"
    "TrustServerCertificate=no;"
    "Connection Timeout=30;"
)
```

The project validated:
1. DNS resolution
2. TCP connectivity to port `1433`
3. installed ODBC drivers
4. direct SQL authentication
5. API-level health endpoint response

---

## 16. SQL Firewall Troubleshooting

The Korea Central deployment initially could not connect to Azure SQL.

The issue was caused by Azure SQL firewall restrictions.

The App Service outbound IP addresses were obtained from Azure Portal and allowed through the SQL server firewall.

After adding the required IP addresses, SQL connectivity from the Korea backend succeeded.

This demonstrates an important cloud networking principle: a service may be running correctly while still being unable to access another Azure service because of network or firewall restrictions.

---

## 17. Telemetry and Resource-Management Agent

GigTrust includes a basic resource-management decision component.

The system can evaluate telemetry such as:
- CPU utilization
- latency
- throughput

Example normal workload:

```text
Region       : Central India
CPU          : 72.4%
Latency      : 118 ms
Throughput   : 430 RPS
Decision     : NO_ACTION
```

Example high-load workload:

```text
Region       : Central India
CPU          : 88.5%
Latency      : 220 ms
Throughput   : 910 RPS
Decision     : SCALE_UP
```

When another region is considered healthier, the agent can produce:

```text
Decision : REROUTE
Target   : Secondary region
```

Decisions are stored in Cassandra so that the system has an auditable record of why resource-management actions were recommended.

---

## 18. Multi-Region Routing

Azure Traffic Manager provides DNS-based routing between the two deployments.

Traffic Manager profile:

```text
gigtrust-global
```

DNS name:

```text
gigtrust-global.trafficmanager.net
```

Routing method:

```text
Priority
```

Configured endpoints:

```text
Priority 1 → Central India
Priority 2 → Korea Central
```

This means the primary endpoint is returned while it is healthy. If the primary endpoint becomes unavailable, Traffic Manager can resolve the secondary endpoint.

---

## 19. Traffic Manager Health Probe

Traffic Manager checks application availability using an HTTPS health probe.

Configuration:

```text
Protocol            : HTTPS
Port                : 443
Path                : /health/database
Probe interval      : 30 seconds
Tolerated failures  : 3
Timeout             : 10 seconds
```

Both endpoints were eventually reported as `Online`.

A dedicated `/health` endpoint returning appropriate HTTP status codes would be a better production design and is listed as a future improvement.

---

## 20. Failover Test

The project performed a real routing test.

### Normal State

Central India was enabled as Priority 1.

Running:

```powershell
Resolve-DnsName gigtrust-global.trafficmanager.net
```

returned the Central India application hostname.

Conceptually:

```text
gigtrust-global.trafficmanager.net
                  ↓
        Central India backend
```

### Failure Simulation

The Central India Traffic Manager endpoint was temporarily disabled.

After DNS propagation, the same command returned:

```text
gigtrust-korea-gzc3dbaqe8grbnf4.koreacentral-01.azurewebsites.net
```

Conceptually:

```text
gigtrust-global.trafficmanager.net
                  ↓
          Korea Central backend
```

### Recovery

The Central India endpoint was re-enabled.

After the DNS TTL expired and Traffic Manager refreshed its routing decision, DNS resolution returned to the primary Central India endpoint.

Therefore the full sequence was demonstrated:

```text
NORMAL
Central India
      ↓
PRIMARY DISABLED
Korea Central
      ↓
PRIMARY RESTORED
Central India
```

This verified **priority-based multi-region failover and recovery**.

---

## 21. Why the Traffic Manager URL Is Not Used as a Normal HTTPS Reverse Proxy

Azure Traffic Manager is a **DNS-based routing service**. It does not terminate HTTPS traffic like a reverse proxy.

Therefore `gigtrust-global.trafficmanager.net` returns the hostname of a backend, but the TLS certificate on each backend belongs to that backend's Azure hostname.

For a production single public hostname, a custom domain and suitable TLS configuration would be required.

For the academic demonstration, DNS resolution provides clear proof that Traffic Manager changed the selected region.

---

## 22. Azure Front Door Limitation

Azure Front Door was originally considered for global routing.

However, the Azure for Students subscription returned an error indicating that Student/Free Trial subscriptions were not allowed to create the Front Door resource.

The project therefore adopted Azure Traffic Manager.

This still demonstrates:
- global service discovery
- health monitoring
- primary/secondary routing
- regional failover

---

## 23. Azure Subscription Limitations Encountered

Several implementation decisions were influenced by Azure for Students policy restrictions.

### Restricted deployment regions
Some Azure regions were blocked by subscription policy.

The allowed list observed during the project included:

```text
Malaysia West
Indonesia Central
Central India
Austria East
Korea Central
```

### Container Apps Environment limit
The subscription allowed only one global Container Apps Environment.

Therefore:
- Central India → Azure Container Apps
- Korea Central → Azure App Service for Containers

### Azure Front Door restriction
Azure Front Door creation was blocked for the Student subscription, so Azure Traffic Manager was used.

These constraints were handled by adapting the architecture instead of abandoning the multi-region requirement.

---

## 24. Cloud Computing Concepts Demonstrated

### Containerization
Docker provides a portable deployment unit.

### Container Registry
Azure Container Registry provides centralized image storage.

### Platform as a Service
Azure Container Apps and App Service abstract infrastructure management while hosting the application.

### Multi-Region Deployment
The same backend is available from two geographically distinct Azure regions.

### Fault Tolerance
A secondary region is available when the primary region is unavailable.

### Health Monitoring
Traffic Manager checks endpoint health.

### DNS-Based Load Routing
Traffic Manager controls which backend hostname is returned.

### Scalability
The architecture supports scaling application instances and storing telemetry for resource-management decisions.

### Cloud Networking
SQL firewall configuration demonstrated how application connectivity depends on network policy.

### Configuration Management
Environment variables and Azure secrets separate code from deployment configuration.

---

## 25. Complete Cloud Deployment Pipeline

```text
Developer writes FastAPI code
            ↓
Docker image is built locally
            ↓
Image tested locally
            ↓
Image tagged for Azure Container Registry
            ↓
Image pushed to ACR
            ↓
      ┌─────┴─────┐
      │           │
      ▼           ▼
Central India    Korea Central
Container Apps   App Service
      │           │
      └─────┬─────┘
            │
            ▼
 Azure Traffic Manager
 Priority-based routing
            │
   ┌────────┼────────┐
   ▼        ▼        ▼
Azure SQL  MongoDB  Cassandra
```

---

## 26. Cloud Resource Summary

| Component | Azure Resource / Technology | Purpose |
|---|---|---|
| Backend | FastAPI | REST application |
| Containerization | Docker | Portable application package |
| Registry | Azure Container Registry | Store Docker image |
| Primary Compute | Azure Container Apps | Central India deployment |
| Secondary Compute | Azure App Service | Korea Central deployment |
| Global Routing | Azure Traffic Manager | Priority-based DNS routing |
| Relational DB | Azure SQL Database | Transactional marketplace data |
| Document DB | Cosmos DB MongoDB API | Activity logs |
| Wide-column DB | Cosmos DB Cassandra API | Telemetry and scaling actions |

---

## 27. Example Deployment Configuration

### Central India

```text
Service     : Azure Container Apps
App         : gigtrust-central
Region      : Central India
Port        : 8000
Image       : gigtrust-api:latest
```

### Korea Central

```text
Service     : Azure App Service
App         : gigtrust-korea
Region      : Korea Central
Port        : 8000
Image       : gigtrust-api:latest
```

### Traffic Manager

```text
Profile     : gigtrust-global
Routing     : Priority
Primary     : Central India
Secondary   : Korea Central
```

---

## 28. Local Development Setup

### Prerequisites

Install:
- Python 3.x
- Git
- Docker Desktop
- ODBC Driver 18 for SQL Server
- Visual Studio Code

Clone the repository:

```bash
git clone https://github.com/Jignesh160/GigTrust.git
cd GigTrust
```

Create a virtual environment:

```bash
python -m venv .venv
```

Activate it on Windows:

```powershell
.venv\Scripts\Activate.ps1
```

Install dependencies:

```bash
pip install -r requirements.txt
```

---

## 29. Environment File

Create a `.env` file in the project directory.

Example:

```env
SQL_SERVER=gigtrust-sql-24222.database.windows.net
SQL_DATABASE=gigtrustdb
SQL_USERNAME=<your-sql-username>
SQL_PASSWORD=<your-sql-password>

MONGO_URI=<your-mongodb-connection-string>
MONGO_DATABASE=gigtrust
MONGO_COLLECTION=activity_logs

CASSANDRA_HOST=gigtrust-cassandra-24222.cassandra.cosmos.azure.com
CASSANDRA_PORT=10350
CASSANDRA_USERNAME=gigtrust-cassandra-24222
CASSANDRA_PASSWORD=<your-cassandra-primary-key>
CASSANDRA_KEYSPACE=gigtrust
```

Never commit `.env` to GitHub.

Ensure `.gitignore` contains:

```gitignore
.env
.venv/
__pycache__/
*.pyc
```

---

## 30. Running Without Docker

Example:

```bash
uvicorn main:app --reload
```

or use the actual module path used by the project.

Then open:

```text
http://127.0.0.1:8000/docs
```

---

## 31. Running With Docker

Build:

```bash
docker build -t gigtrust-api .
```

Run:

```bash
docker run --rm -p 8001:8000 --env-file .env gigtrust-api
```

Open:

```text
http://localhost:8001/docs
```

---

## 32. Pushing the Image to Azure Container Registry

Login:

```bash
docker login gigtrustacr24222-b2bhamdhfhfth2gh.azurecr.io
```

Tag:

```bash
docker tag gigtrust-api:latest gigtrustacr24222-b2bhamdhfhfth2gh.azurecr.io/gigtrust-api:latest
```

Push:

```bash
docker push gigtrustacr24222-b2bhamdhfhfth2gh.azurecr.io/gigtrust-api:latest
```

---

## 33. Testing Database Connectivity

Useful API checks:

```text
GET /health/database
GET /health/mongodb
GET /health/cassandra
```

The expected result is that all services report a connected/healthy state.

---

## 34. Testing Traffic Manager Routing

PowerShell:

```powershell
Resolve-DnsName gigtrust-global.trafficmanager.net
```

Normal result should resolve toward Central India.

For failover testing:

1. disable the `central-india` Traffic Manager endpoint
2. wait for endpoint status/DNS TTL propagation
3. resolve the hostname again
4. verify Korea Central is returned
5. re-enable Central India
6. wait for propagation
7. verify the DNS result returns to Central India

If DNS remains cached:

```powershell
Clear-DnsClientCache
```

or:

```powershell
ipconfig /flushdns
```

A public resolver can also be queried:

```powershell
Resolve-DnsName gigtrust-global.trafficmanager.net -Server 8.8.8.8
```

---

## 35. Security Considerations

The project follows several basic cloud-security practices.

### Secrets
Passwords and connection strings are not intended to be embedded directly in source code.

### Encryption
Azure SQL connections use:

```text
Encrypt=yes
TrustServerCertificate=no
```

### Network restrictions
Azure SQL firewall controls which sources may connect.

### Future security improvements
- Azure Key Vault
- Managed Identity
- RBAC
- private endpoints
- VNet integration
- Web Application Firewall
- API authentication using OAuth/JWT
- secret rotation
- centralized logging

---

## 36. Troubleshooting Lessons

### Docker daemon unavailable
Resolved by installing/configuring WSL and Docker Desktop.

### Docker image build failed
Resolved by correcting the Linux base image/repository compatibility required by Microsoft ODBC packages.

### ACR hostname error
The initial registry hostname contained a typo. Correcting the generated secure ACR login server fixed DNS/login failures.

### Azure SQL inaccessible from deployed application
Connectivity was tested at multiple levels:

```text
DNS
→ TCP port 1433
→ ODBC driver
→ SQL login
→ application API
```

### Korea App Service SQL timeout
Resolved by adding required App Service outbound IP addresses to the Azure SQL firewall.

### Second Container Apps environment blocked
Solved by using Azure App Service for Containers for the second region.

### Azure Front Door unavailable
Solved by switching to Azure Traffic Manager.

### Traffic Manager endpoints degraded
Probe routing/host behavior was corrected until both endpoints reported Online.

---

## 37. Results

The project successfully demonstrated:

- FastAPI running locally
- Dockerized backend
- image stored in Azure Container Registry
- primary backend running in Central India
- secondary backend running in Korea Central
- Azure SQL connectivity from both deployments
- MongoDB connectivity from both deployments
- Cassandra connectivity from both deployments
- Traffic Manager endpoints Online
- priority-based routing
- primary-region failover
- recovery to primary region
- telemetry storage
- scaling-action audit records

---

## 38. Demonstrated Failover Result

The most important cloud experiment was:

```text
Initial state
Traffic Manager
      ↓
Central India

Primary endpoint disabled
Traffic Manager
      ↓
Korea Central

Primary endpoint restored
Traffic Manager
      ↓
Central India
```

This demonstrates that the system can maintain application availability across regions at the routing layer.

---

## 39. Current Limitations

The current project is an academic prototype and has several limitations.

- Traffic Manager is DNS-based rather than a reverse proxy.
- The Traffic Manager hostname is not configured as a shared custom TLS domain.
- Health probing currently uses a database-specific endpoint.
- Resource-management actions are recommendation/simulation oriented rather than fully controlling Azure scaling resources.
- Frontend development is still planned.
- Authentication and production authorization require further hardening.
- Full managed-identity integration is not yet implemented.
- Databases are shared cloud services rather than fully duplicated data stacks per application region.

---

## 40. Future Work

### Frontend
A React-based frontend can provide:
- login and registration
- client dashboard
- freelancer dashboard
- gig creation
- bid submission
- contract status
- escrow status
- health/monitoring dashboard

### Authentication
- JWT authentication
- password hashing
- client/freelancer/admin roles

### Fraud Detection
A machine-learning fraud detection module can generate a risk score for suspicious marketplace activity.

Potential features:
- unusual login location
- repeated failed payments
- abnormal bid behavior
- rapid account changes
- high-value transaction anomalies

### Explainable AI
SHAP or similar methods can explain why an event was classified as suspicious.

### Unified Health Endpoint
Create `/health` that returns:

```text
HTTP 200 → healthy
HTTP 503 → unhealthy
```

### Azure Key Vault
Move all production secrets into Azure Key Vault.

### Managed Identity
Use Managed Identity instead of long-lived credentials where supported.

### Automated Scaling Integration
Resource-agent decisions could be connected to real Azure scaling APIs.

### Custom Domain
Configure a custom domain shared across both origins.

### Production Observability
Add:
- Application Insights
- Azure Monitor alerts
- centralized logs
- dashboards

---

## 41. Suggested Demonstration Flow

```text
1. Show GitHub repository
2. Explain architecture
3. Show Dockerfile
4. Show Azure Container Registry image
5. Open Central India FastAPI Swagger
6. Test Azure SQL health
7. Test MongoDB health
8. Test Cassandra health
9. Open Korea Central FastAPI Swagger
10. Verify all three databases there
11. Open Azure Traffic Manager
12. Show both endpoints Online
13. Resolve Traffic Manager DNS → Central India
14. Disable Central India endpoint
15. Resolve DNS again → Korea Central
16. Re-enable Central India
17. Resolve DNS again → Central India
18. Show Cassandra telemetry/scaling-action records
19. Explain subscription limitations and design decisions
20. Conclude with fault tolerance and future improvements
```

---

## 42. Key Viva Points

### Why Azure SQL?
Escrow and marketplace transactions require ACID properties, strong consistency, referential integrity, and transaction support.

### Why MongoDB?
Activity logs are semi-structured and can evolve over time, making a document database appropriate.

### Why Cassandra?
Telemetry is high-volume, time-ordered, write-heavy data. Cassandra's partitioning and clustering model is suitable for this pattern.

### Why Docker?
Docker creates a portable, reproducible application image that can be deployed to different compute platforms.

### Why Azure Container Registry?
It provides private centralized storage for Docker images used by Azure compute services.

### Why two regions?
Multi-region deployment improves availability and demonstrates fault tolerance.

### Why Traffic Manager?
Traffic Manager provides DNS-based global routing and health-aware failover.

### Why Priority routing?
One region acts as primary while the other remains available as a backup.

### Why Central India as primary?
It is geographically appropriate for the primary target environment and was available under the student subscription.

### Why Korea Central for the second region?
Korea Central was one of the regions permitted by the Azure for Students policy.

### Why App Service in Korea instead of Container Apps?
The subscription allowed only one global Container Apps Environment, so App Service provided a practical second-region container host.

### Why not Azure Front Door?
Azure Front Door creation was blocked by the Azure for Students subscription.

### What proves failover worked?
`Resolve-DnsName gigtrust-global.trafficmanager.net` changed from the Central India backend to the Korea Central backend when the primary endpoint was disabled, and returned to Central India after recovery.

---

## 43. Repository Structure

The exact structure may evolve, but a clean organization is:

```text
GigTrust/
│
├── app/
│   ├── api/
│   ├── db/
│   ├── models/
│   ├── services/
│   └── main.py
│
├── scripts/
│   ├── activity_logs.py
│   ├── telemetry_client.py
│   └── verify_all.py
│
├── sql/
│   ├── 01_schema.sql
│   └── escrow_transactions.sql
│
├── cassandra/
│   └── telemetry_schema.cql
│
├── docs/
│   └── project documentation
│
├── Dockerfile
├── requirements.txt
├── .gitignore
├── .env.example
└── README.md
```

Adjust the structure above to match the actual repository if filenames differ.

---

## 44. Project Status

### DBMS
**Core DBMS implementation: Completed**

Implemented concepts include:
- relational database design
- normalization
- ACID transactions
- escrow logic
- rollback
- concurrency concepts
- deadlock handling concepts
- MongoDB activity logging
- Cassandra telemetry storage
- auditable scaling actions

### Cloud Computing
**Core cloud implementation: Completed**

Implemented concepts include:
- Docker containerization
- Azure Container Registry
- Azure Container Apps
- Azure App Service
- multi-region deployment
- Traffic Manager
- health probes
- Azure SQL cloud connectivity
- MongoDB cloud connectivity
- Cassandra cloud connectivity
- failover
- recovery
- telemetry-driven resource-management logic

### Remaining Application Work
The main remaining development area is the full user-facing application layer:
- frontend
- end-to-end marketplace workflows
- authentication
- automatic fraud scoring
- explainable AI
- production-grade health monitoring and secrets management

---

## 45. Conclusion

GigTrust demonstrates how a freelance marketplace can be designed using both traditional database principles and modern cloud architecture.

The DBMS layer ensures that important marketplace operations such as escrow transactions maintain consistency and reliability. MongoDB and Cassandra extend the architecture with flexible logging and high-volume telemetry storage.

The cloud layer packages the application as a Docker container, stores it in Azure Container Registry, deploys the same application across Central India and Korea Central, and uses Azure Traffic Manager for priority-based failover.

The successful failover test from Central India to Korea Central and back to Central India demonstrates that the architecture is capable of maintaining service availability during a regional failure scenario.

The project therefore combines:

```text
Database Reliability
        +
Polyglot Persistence
        +
Containerization
        +
Multi-Region Deployment
        +
Health Monitoring
        +
Failover
        +
Telemetry-Based Resource Management
```

into a single cloud-native academic prototype.

---



